<%= form_for(book_detail) do |f| %>
  <% if book_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_detail.errors.count, "error") %> prohibited this book_detail from being saved:</h2>

      <ul>
      <% book_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %>
    <%= f.number_field :id %>
  </div>

  <div class="field">
    <%= f.label :book_title %>
    <%= f.text_field :book_title %>
  </div>

  <div class="field">
    <%= f.label :publication_year %>
    <%= f.number_field :publication_year %>
  </div>

  <div class="field">
    <%= f.label :language %>
    <%= f.text_field :language %>
  </div>

  <div class="field">
    <%= f.label :category_type %>
    <%= f.number_field :category_type %>
  </div>

  <div class="field">
    <%= f.label :binding_id %>
    <%= f.number_field :binding_id %>
  </div>

  <div class="field">
    <%= f.label :no_of_copies_stored %>
    <%= f.number_field :no_of_copies_stored %>
  </div>

  <div class="field">
    <%= f.label :no_of_copies_present %>
    <%= f.number_field :no_of_copies_present %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
